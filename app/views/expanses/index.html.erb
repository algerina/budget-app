<div>
  <% @expanses.each do |expanse|%>
    <div>
      <h3><%= expanse.name%></h3>
      <small><%= expanse.creation_date.strftime("%d %b %Y") %></small>
      <em>$<%= expanse.amount %></em>
    </div>
  <% end %>
  "Hello"
</div>